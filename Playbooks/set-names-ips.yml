---
- name: Set new hostname and IP
  hosts: 192.168.29.109
  
  tasks:
  - name: update hostnames
    hostname:
      name: tarro1

  - name: Set a hostname
    ansible.builtin.hostname:
      name: tarro2